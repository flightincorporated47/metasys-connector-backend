# Install Notes (Windows)
**Created:** 2026-01-01

You asked for these SOPs to live in:

- `C:\Users\gduarte\Downloads\metasys-connector-main\metasys-connector\docs`
- and also be copied into the connector `exports` folder

This download package is already structured as:

- `metasys-connector\docs\*.md`
- `metasys-connector\exports\sops\*.md`

## How to install
1. Download and unzip this package.
2. Copy the `metasys-connector` folder into:
   `C:\Users\gduarte\Downloads\metasys-connector-main\`
3. If Windows asks to merge/replace folders, choose **Merge** (and replace only if prompted for the same files).

That will land the SOPs in both locations automatically.

